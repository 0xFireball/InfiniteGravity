using Microsoft.Xna.Framework;

namespace InfiniteGravity.Components.Characters.Gear {
    public class Sword : MeleeWeapon {
        public Sword(Vector2 offset, Rectangle reach) : base(offset, reach) { }
    }
}